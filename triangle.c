#include <stdio.h>

main () {

    int n;
    scanf("%d", &n);
    for (int i = 0; i < n; i++) {
        if (i%2== ){
        for (int j = 0; j < n-i; j++) {
             printf("*");
        } eise {    
            printf("+");
        }
        printf("\n");
    }
}
